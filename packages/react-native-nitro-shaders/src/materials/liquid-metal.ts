export const LIQUID_METAL_DEFAULTS = {
  repetition: 4,
  softness: 0.5,
  shiftRed: 0.3,
  shiftBlue: 0.3,
  distortion: 0.1,
  contour: 0.5,
  angle: 0,
  speed: 1,
}

export type LiquidMetalShape = 'none' | 'circle' | 'daisy' | 'diamond' | 'metaballs'
